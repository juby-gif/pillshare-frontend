

export function postTimeSeriesDatum(postData,onSuccessfulCallBack,onFailureCallBack){

    

    // Structure of how're we gonna save the data to the user?

    console.log(postData.token);
    console.log(postData.user_id);
    console.log(postData.heartRateData);
    console.log(postData.bloodPressureData);
    console.log(postData.bodyTemperatureData);
    console.log(postData.glucoseData);
    console.log(postData.oxygenSaturationData);
    }